# Copy to backend/.env and fill in values. Never commit the real .env.
GOOGLE_API_KEY=your-gemini-api-key-here
# Absolute paths preferred; repo-relative backend/models + backend/data are the defaults.
MODEL_DIR=/home/krish/Panamax/backend/models
DATA_DIR=/home/krish/Panamax/backend/data
GEMINI_MODEL=gemini-2.5-flash
